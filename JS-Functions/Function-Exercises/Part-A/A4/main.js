// A4 – formatStudent
function formatStudent(name, group, gpa) {
  return `Student ${name} from ${group} has GPA ${gpa}`;
}

// Example
console.log(formatStudent("Aida", "SE-101", 3.8));
// "Student Aida from SE-101 has GPA 3.8"